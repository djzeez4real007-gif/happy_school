/// Firebase is prepared but **not forced on**.
/// Hive remains the working database until you complete Firebase setup.
class FirebaseConfig {
  /// Keep false until flutterfire configure is done — avoids crash on startup.
  static const bool enabled = false;
}
